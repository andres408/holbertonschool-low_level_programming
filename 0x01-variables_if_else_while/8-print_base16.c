#include<stdio.h>
/**
 * main - Display all hexadecimal numbers
 *
 * Return : Always 0 (Succes)
 */
int main(void)
{
int i;
for (i = 48; i <= 57; i++)

putchar (i);
for (i = 97; i <= 102; i++)

putchar (i);
putchar ('\n');
return (0);
}
